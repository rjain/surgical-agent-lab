.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
# Any *.env, not just the canonical one: a key in a differently-named file is
# still a key, and `git add -A` does not know the difference.
*.env
!.env.example
data/
.cache/

# adk web keeps its session store next to the agent
adk_agents/**/.adk/
